/-
Machine-checked results for the thesis paper (`Paper/iteration4`).

Each module mirrors one tex file:

* `LeanProject.Preliminaries` ↔ `preliminaries.tex`
* `LeanProject.IV`            ↔ `iv.tex`
* `LeanProject.RoM`           ↔ `rom.tex`
* `LeanProject.MoR`           ↔ `mor.tex`
* `LeanProject.Inference`     ↔ `inference.tex`

See the header of each module for the list of paper statements it verifies.
-/
import LeanProject.Preliminaries
import LeanProject.IV
import LeanProject.RoM
import LeanProject.MoR
import LeanProject.Inference
